# Language Settings
- Always respond in Japanese.
- Code comments and documentation must be written in Japanese.

# 9スペシャリスト・マルチエージェント体制 & チケット管理運用ルール

本プロジェクト（ProjectRouteX）では、以下の 9 名のスペシャリスト・エージェントが協調して開発・運用を行います。

## 1. エージェント一覧と担当フォルダ
1. **プロジェクトマネージャー** (`team/01_project_manager/`)
2. **3PL業界アナリスト** (`team/02_3pl_analyst/`)
3. **WMSスペシャリスト** (`team/03_wms_specialist/`)
4. **開発アーキテクチャー** (`team/04_software_architect/`)
5. **データーベースアナリスト** (`team/05_database_analyst/`)
6. **インフラ設計者** (`team/06_infrastructure_engineer/`)
7. **UIデザイナー** (`team/07_ui_designer/`)
8. **セキュリティ管理者** (`team/08_security_administrator/`)
9. **テスト設計者** (`team/09_test_engineer/`)

## 2. 開発運用ルール
- **隠し事ゼロの原則**: 各エージェントは発見・設計・修正内容を必ず自身の `KNOWLEDGE.md` に蓄積し、オープンに共有します。
- **チケット管理**: 全タスクは `team/tickets/BOARD.md` および個別チケット (`TICK-xxx.md`) で管理し、進捗率とステータスを可視化します。
- **マルチエージェント協議と参加エージェント表示の徹底（絶対ルール・最重要）**:
  - 仕様決定、設計提案、機能実装、インフラ変更を行う際は、必ず関連するスペシャリスト・エージェント間で協議を行い、合意の上で決定しなければならない。
  - ユーザーへの回答・提案・報告時には、必ず**「協議に携わったエージェント一覧」を明示**すること。
- **横展開と再発防止の徹底（最重要）**: 
  - 特定の機能や画面でバグ修正・仕様改善・UI機能追加（エラーメッセージ日本語化、プログレスアニメーション表示等）を行った場合、担当エージェントは即座に同パターンの処理・画面が他機能に存在しないか横断検索（`grep`等）を行い、全箇所へ漏れなく横展開修正・UI統一を行わなければならない（特定画面のみでの限定実装は障害・不統一とみなす）。
  - 再発防止策として、修正理由と共通設計ルールをプロジェクトの `GEMINI.md` および担当エージェントの `KNOWLEDGE.md` に即時知識化・更新する。
- **テストデータ自動投入の絶対禁止原則（最重要・厳守）**:
  - テストデータやサンプルデータの生成・投入処理をプログラム内（`DbInitializer` や起動ロジック、コントローラー等）に埋め込んだり、アプリ起動時に自動実行することは**全面禁止**とする。
  - テストデータの流し込みは、ユーザーから明示的に「テストデータを流し込んで」と指示があった場合のみ、プログラム本体とは分離された独立スクリプト・手動実行ツールで実施しなければならない。
- **動作検証データの即時クリーンアップ・削除原則（厳守）**:
  - 開発・機能修正・動作検証時に作成したテスト用データ（DBレコード、テスト用CSV/Excelファイル等）は、検証作業が完了した直後に必ずすべて削除・クリーンアップし、環境をクリーンな状態に復元しなければならない。